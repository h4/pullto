language: python
python:
  - "2.7"
# command to install dependencies
# install: "pip install -r pip-requirements --use-mirrors"
# command to run tests
script: nosetests
# run deploy script on server
notifications:
  webhooks:
    urls:
       - http://home.brnv.ru:8080/
    on_sucess: always
    on_failure: never